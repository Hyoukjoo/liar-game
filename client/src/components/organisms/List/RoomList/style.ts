import { GlassList } from '@atoms/List';
import styled from '@emotion/styled';

namespace RoomListStyle {
  export const List = styled(GlassList)`
    min-height: 210px;
    max-height: 270px;
  `;
}

export default RoomListStyle;
