import styled from '@emotion/styled';

namespace BaseListStyle {
  export const List = styled.ul`
    min-height: 125px;
    max-height: 225px;
    overflow-x: hidden;
    overflow-y: scroll;
    list-style: none;
  `;
}

export default BaseListStyle;
