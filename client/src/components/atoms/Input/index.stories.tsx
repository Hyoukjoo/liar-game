import { BaseInput, TextInput } from '.';

export default {
  title: 'ATOM/INPUT',
};

export const Base_Input = () => {
  return <BaseInput />;
};

export const Text_Input = () => {
  return <TextInput />;
};
