import React from "react";
import UnderlineInput from ".";
export default { title: "Basic/Input" };

export const Underline = () => {
  return <UnderlineInput placeholder='이름 입력' maxLength={10} />;
};
